<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <link href="/css/output.css" rel="stylesheet" />
  </head>
  <body>
    <%- include("../includes/header.ejs") %>

    <main class="flex flex-col items-center">
      <div class="flex mt-12 w-[1200px]">
        <div class="w-6/12"><img src="<%= product.imageUrl %>" /></div>
        <div class="w-6/12">
          <h1 class="text-9xl font-bold uppercase ml-8">
            <%= product.title %>
          </h1>
          <p class="ml-8 mt-8"><%= product.description %></p>
          <form action="/add-to-cart" method="post">
            <input hidden name="productId" value="<%= product._id %>" />
            <input hidden name="productPrice" value="<%= product.price %>" />
            <button
              class="uppercase bg-lime-900 w-[128px] mt-8 ml-8 text-white pt-2 pb-2 hover:translate-y-1 hover:cursor-pointer transition duration-75"
              type="submit"
            >
              Add to cart
            </button>
          </form>
        </div>
      </div>
    </main>
  </body>
</html>
